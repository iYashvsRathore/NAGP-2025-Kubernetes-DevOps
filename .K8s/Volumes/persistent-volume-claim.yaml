apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sqlserver-data-persistent-volume-claim
spec:
  storageClassName: database-storage-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
